version: "2"
sql:
  - engine: "postgresql"
    queries: "queries/"
    schema: "../internal/db/migrations/"
    gen:
      go:
        package: "sqlcgen"
        out: "../internal/db/sqlcgen"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
